"""Intervals.icu MCP server.

This package is self-contained: it has no imports from the legacy StravaGPT
modules (``app.strava``, ``app.storage``, ``app.config``) so the Strava side of
the project can be removed without touching anything here.
"""
